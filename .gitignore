/.phpunit.cache
/.phpunit.result.cache
/node_modules
/public/build
/public/hot
/public/storage
/storage/*.key
/storage/pail
/vendor
.env
.env.backup
.env.production
.phpactor.json
auth.json
npm-debug.log
yarn-error.log
/.fleet
/.idea
/.nova
/.vscode
/.zed

# Banco SQLite local (gerado no setup)
/database/database.sqlite
/database/*.sqlite-journal
# Arquivos auxiliares do modo WAL (Fase A4) — efêmeros, nunca versionar
/database/*.sqlite-wal
/database/*.sqlite-shm

# Backups locais gerados por scripts/backup-sqlite.sh
/storage/app/backups

# Bundles offline gerados por build-bundle.ps1
/build
